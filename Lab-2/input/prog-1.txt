def repeatCallExample() of int
    {
        {
            f(g(h(x,y())));
        } until x < 3;

    } while t < d;
end



def main() of int
    x = t;
    while a < b
        while c < d
            if x > y then {
                foo();
                break;
                bar();
            }
            else {
                baz();
            }
            phi();
            break;
            psi();
    end
    omega();
    end
end
