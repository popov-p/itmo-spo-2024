def main() of int
    while t < s
        while psi < phi
            if a < z then {
                foo();
                break;
                baz();
            }
            psi();
        end
        must();
    end
    foo();
    asddas();
end
